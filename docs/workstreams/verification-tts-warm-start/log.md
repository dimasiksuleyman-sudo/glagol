# Проверка быстрого запуска Silero — журнал

Журнал append-only. Даты UTC. Ручные результаты отмечаются manual и не
превращаются в observed без независимой проверки.

## Начало — 2026-09-15T09:34:38.196Z

Пользователь разрешил пошаговую A/B-проверку ответом «го». Baseline Git:
dbb56d5, ветка main; рабочее дерево содержит только незакоммиченный результат
закрытой серии tts-warm-start и её документацию. Незнакомых изменений нет,
индекс пуст. Старая verification-0.4.0 остаётся paused и не изменяется.

Локальный candidate зафиксирован до запуска: `src-tauri/target/release/glagol.exe`,
26 627 072 байта, LastWriteTimeUtc 2026-09-15 06:26:25, SHA-256
ebe1f51c612a0690742a6952ff2647141276756c526178ffa4c260ab5e252195.
NSIS candidate: 9 754 593 байта, SHA-256
3798649e722789a32cf7a42cca39fc6b88018be34b0cb208ba0cbdd6d7275ee8;
он пока не устанавливается.

Q1 занят исполнителем codex. Сначала read-only идентификация установленной версии,
процесса, Silero и логов; затем пользователь создаёт штатный бэкап библиотеки.

## Q1, среда — 2026-09-15T09:36:23.031Z

Read-only проверка: Windows Core 25H2 build 26200.9457 x64, AMD Ryzen 7 7730U;
CIM запрещён политикой, те же необходимые поля прочитаны из системного реестра.
Установленный `C:/Users/kiss2/AppData/Local/Glagol/glagol.exe`: версия 0.4.0,
26 520 576 байт, SHA-256
5b93bb8c0df8b027de2ec88f5aae428d7c57bec1fcb95b724b9ca958b3061cea.
Процесс Glagol не запущен. В профиле присутствуют закреплённая модель,
`runtime/python.exe` и acknowledgement; содержимое пользовательской библиотеки,
тексты, аудио и ключи не читались.

В технических release-логах старой версии найдены только времена загрузки worker,
не включая предшествующее полное хеширование: 4156 мс (2026-09-13), 4192 и
3943 мс (2026-09-15). Эти числа — дополнительный baseline загрузки модели, но
не замена ручному времени от действия пользователя до готовности.

Environment PASS. Backup остаётся NOT_RUN до подтверждения пользователя.

## Q1, логирование и бэкап — 2026-09-15T09:41:39.318Z

Перед замерами проверено release-логирование. Фильтр включает
`glagol_lib=info`; rolling appender создаёт дневные `glagol.*.log` и хранит семь
файлов. Существующие события `local TTS worker ready` реально присутствуют.
Новые `local TTS background verification completed` и `local TTS worker ready`
имеют info-уровень и несут только длительность/идентификатор модели. Отдельного
события выгрузки по idle нет: этот критерий будет manual по дочернему python.exe.
Общее воспринимаемое время измеряется отдельно, потому что внутренние события
делят его только на проверку и загрузку модели.

Пользователь сообщил о штатном создании
`glagol-backup-2026-09-15-023832.zip`. Файл найден на OneDrive Desktop:
3 680 байт, LastWriteTimeUtc 2026-09-15 09:38:32, SHA-256
90eca776525801fce20b81b14e8a6487b3bf852ffff28c9e17dd6858f7850552.
ZipArchive: 2 записи, manifest/database присутствуют, все записи читаются.
Содержимое payload не сохранялось в evidence. Backup PASS.

Q1 завершён. Q2 занят исполнителем codex; следующий manual шаг — холодный
baseline установленной опубликованной 0.4.0 после перезагрузки.

Первый context refresh после Q1 записал STATUS, но `runbook:check` вернул exit 1:
у command-проверки ZIP по ошибке остался `exit_code: null`; из-за невалидной
серии валидатор также не мог подтвердить единственную active selection. Поле
исправлено на фактический exit 0; продуктовые файлы и результаты QA не менялись.

## Q2, автоматизация замеров — 2026-09-15T09:50:54.522Z

По запросу пользователя ручной секундомер исключается из основного сценария.
Перед baseline в release-лог добавляются структурированные длительности: готовность
главного окна, полная проверка целостности, подготовка/повторное использование
worker, первая и повторная озвучка и выгрузка по idle. События не должны содержать
текст, аудио, путь документа, ключи или другие пользовательские данные.

## Q2, реализация timing-логов — 2026-09-15T09:59:48.439Z

Добавлены одноразовый маркер первого отображённого кадра главного окна и
структурированные события полного TTS-цикла: фактическая проверка целостности,
доступность фонового результата, загрузка worker, общая подготовка с признаками
повторного использования, preview, синтез документа и выгрузка после idle.
Ошибочные события содержат длительности и тип этапа, но не текст ошибки.
Текст/аудио/пути документов/ключи не логируются; для документа допускается только
число символов. Ранбук дополнен таблицей событий и безопасной командой выборки.

Проверки: Rust fmt и clippy с `-D warnings` PASS; cargo test — 328 PASS,
0 FAIL, 3 ignored hardware/native; встроенный сканер утечек логов PASS;
TypeScript `--noEmit` и production Vite PASS (1914 modules); тесты ранбука —
49 PASS. Первая попытка cargo check без локального PDFium override получила
ожидаемую ошибку `PDFIUM_LIBRARY_PATH not defined`; повтор с документированным
`src-tauri/resources/pdfium.dll` PASS. Это ограничение окружения, не кода.

Release/NSIS build завершён exit 0. Новый candidate, ещё не запускавшийся и не
устанавливавшийся: `src-tauri/target/release/glagol.exe`, 26 727 424 байта,
SHA-256 fa0ffb4404e77af36e444ace1d2fe25a9b0acbb70012c209df8363c4f8ba2717;
NSIS 9 786 816 байт, SHA-256
0179b919e74509d96aca99047b7d993a14661d635751e7b221393850e327c027.
Артефакты из начальной записи серии заменены этой сборкой до первого запуска.
Instrumentation PASS; фактическое появление событий в release-логе остаётся
NOT_RUN до Q3. Q2 остаётся in_progress: впереди baseline опубликованной 0.4.0.

После checkpoint: `pnpm.cmd context:refresh` — exit 0,
`context-status write: OK`; `pnpm.cmd runbook:check` — exit 0,
`runbook-check OK (5 series)`; `git diff --check` — exit 0 с информационными
LF→CRLF. Commit, запуск GUI, установка и публикация не выполнялись.

## Q2, холодный baseline опубликованной 0.4.0 — 2026-09-15T10:20:03.490Z

Пользователь запустил первую озвучку и сообщил: «длилась невероятно долго, так
не годится». В release-логе в 10:17:26Z появилась только старая строка
`local TTS worker ready ... elapsed_ms=3341`. Маркеры новой сборки
`application UI ready`, `integrity verification`, `preparation` и `synthesis`
полностью отсутствуют. Хеш установленного EXE остаётся
5b93bb8c0df8b027de2ec88f5aae428d7c57bec1fcb95b724b9ca958b3061cea, тогда как
candidate имеет SHA-256
fa0ffb4404e77af36e444ace1d2fe25a9b0acbb70012c209df8363c4f8ba2717.

Следовательно, проверен опубликованный baseline через установленный ярлык, а не
candidate. Наблюдаемые 3341 мс — только загрузка worker; старая полная проверка
перед ней не имела timing-события, поэтому точное end-to-end время восстановить
нельзя. Качественный результат baseline — неприемлемая задержка — зафиксирован,
и это ограничение измерения явно сохранено. Q2 завершён, Q3 занят исполнителем
codex: следующий запуск должен быть прямо из release EXE в рабочем дереве.

## Q3, 30-дневная отметка и быстрый повторный запуск — 2026-09-15T10:34:43.410Z

Уточнена сама оптимизация: прежний candidate переносил полную проверку в фон,
но всё равно выполнял её при каждом запуске и блокировал раннюю первую озвучку.
Теперь успешный контроль создаёт `tts_models/verification.json` на 30 дней.
При запуске быстро сверяются возраст отметки, идентификатор модели/встроенной
описи и размер/mtime модели, `python.exe` и `python311.dll`. Отсутствующая,
просроченная или несовпадающая отметка вызывает полный контроль; ошибка worker
сбрасывает отметку и принудительно проверяет файлы перед повторным запуском.

Добавлены тесты свежей, просроченной и изменённой отметки. Полная проверка:
Rust fmt/clippy PASS; cargo test — 330 PASS, 0 FAIL, 3 ignored; сканер утечек
логов PASS; TypeScript/Vite PASS; 49 тестов ранбука PASS. Первый NSIS build был
корректно остановлен блокировкой EXE работающим тестовым candidate PID 16156;
процесс, запущенный codex, завершён, повторная сборка завершилась exit 0.
Новый EXE: 26 693 632 байта, SHA-256
e31d90c934789cc25438a05af4dac0e862e71caf089f484d78ab5a0a5385c96d.
Новый NSIS: 9 778 763 байта, SHA-256
a916653297cfadc89aa72290321abefda4969aab9bb381ee43315745fac1a525.

Первый запуск новой логики начался 10:33:31Z и создал отсутствовавшую отметку
в 10:34:21Z: миграционный полный контроль занял около 50 секунд. Запуск codex
унаследовал `RUST_LOG=warn`, поэтому info-события этого прогона отфильтрованы;
факт и граница получены из времени старта конкретного candidate-процесса и
LastWriteTimeUtc отметки. Процесс перезапущен с `RUST_LOG` unset.

Повторный запуск подтверждён структурированным release-логом: быстрая проверка
отметки 2 мс; `verification_performed=false`; UI ready 805 мс; загрузка worker
3539 мс; общая подготовка/прогрев 3540 мс. Candidate EXE и дочерний
`tts_models/runtime/python.exe` наблюдались запущенными. Полное хеширование на
обычном запуске устранено. Фактический короткий синтез ожидает действия пользователя;
Q3 остаётся in_progress.

## Q3, понятная подготовка в UI — 2026-09-15T10:41:35.840Z

По замечанию пользователя раздел «Озвучить» больше не показывает необъяснимую
серую кнопку. Пока начальный `prepare_tts` не завершён, над формой виден отдельный
status-callout: вращающийся значок, движущийся indeterminate progress bar и текст
о нормальных ~4 секундах и возможной ежемесячной проверке до минуты. Сама кнопка
подписана «Подготовка озвучки…». Аналогичный indeterminate bar используется на
этапе `preparing` запущенного синтеза; после разбиения текста остаётся прежний
процентный прогресс по фрагментам. Учтён `prefers-reduced-motion`.

TypeScript `--noEmit`, version check и production Vite build PASS, 1914 modules.
Tauri/NSIS build завершён exit 0. Новый EXE: 26 693 632 байта, SHA-256
b10060f90be6cf090aa770df0a0ec9f7936c4b18e9dca8363e5341b2f1bfe562;
NSIS: 9 779 374 байта, SHA-256
04aa4de7a829376c1292f53e0f6235a30f8be0a56ce01b19a1873d6a59a34fdd.
`git diff --check` PASS с информационными LF→CRLF.

Для визуального QA применён Computer Use skill, но Windows native pipe оказался
недоступен после двух лёгких повторов и сброса JS kernel; согласно recovery
процедуре дальнейшая автоматизация остановлена. Visual UI остаётся NOT_RUN до
прямого просмотра пользователем; компиляция не выдаётся за визуальную проверку.

После пересборки candidate запущен в 10:42:27Z. Release-лог подтвердил: быстрая
сверка отметки 3 мс, UI ready 637 мс, загрузка worker 3585 мс, весь прогрев
3586 мс, `verification_performed=false`. Также обнаружен уже выполненный
пользователем candidate-синтез в 10:35:30Z: 806 символов, preparation 0 мс,
synthesis 5232 мс, total 5233 мс. Текст и аудио не читались и не фиксировались.
Candidate timing PASS; окно новой UI-сборки сейчас открыто, visual UI ожидает
подтверждения пользователя.
## Q3, визуальное подтверждение — 2026-09-15T10:49:14.472Z

Пользователь визуально проверил перестроенный candidate и подтвердил результат
словом «отлично». Статус подготовки, движущийся progress bar и понятная подпись
недоступной кнопки приняты как manual PASS. Предыдущий NOT_RUN относился только
к недоступности автоматического захвата окна и заменён прямым наблюдением
пользователя. Q3 завершён; Q4 занят codex для проверки повторного запуска,
15-минутной выгрузки worker и границ регрессии диктовки.

## Q4, повторное использование, idle и диктовка — 2026-09-15T11:00:40.000Z

После прогрева две последующие подготовки заняли 0 мс и повторно использовали
тот же worker. Дополнительная озвучка 384 символов: preparation 0 мс,
synthesis/total 2574 мс. В 11:00:28Z release-лог зафиксировал штатную выгрузку
worker через 901 926 мс простоя (≈15 мин 2 с). Дочерний Python PID 6184 уже
отсутствовал, а Glagol PID 8576 продолжал работать; затем codex остановил только
этот запущенный им candidate перед релизной сборкой.

Отдельный `cargo test ... dictation -- --nocapture` завершился exit 0:
146 PASS, 0 FAIL, 1 ignored live-microphone, 186 filtered out. Свежая ручная
диктовка через микрофон именно в candidate не выполнялась и остаётся NOT_RUN;
ignored hardware test не объявляется пройденным. Изменения ограничены TTS и
индикацией его подготовки, однако это лишь оценка области риска, а не ручной
PASS. Q4 завершён; Q5 занят codex для итогового решения и передачи в выпуск.

## Q5, решение — 2026-09-15T11:02:00.000Z

Пользователь подтвердил новую индикацию и прямо поручил подготовить и
опубликовать GitHub Release. По измерениям повторный запуск больше не выполняет
полное хеширование: техническая отметка проверяется за 2–3 мс, UI готов за
637–805 мс, worker прогревается примерно за 3,5 с, повторная подготовка занимает
0 мс. Первая миграционная полная проверка около 50 секунд остаётся разовой,
затем повторяется через 30 дней, при изменении файлов или ошибке worker.

Решение: выпускать отдельный patch release v0.4.1, не перезаписывая v0.4.0.
Fresh live-microphone regression остаётся NOT_RUN и должен быть указан как
ограничение. Серия verification-tts-warm-start завершена; публикация ведётся
отдельной серией release-0.4.1.
